model Task @notionDatabase("aac810fcb3414dbb9c46ce485bc6449b") {
  Name              String    @title @map("Name")
  完了              Boolean?  @checkbox @map("完了")
  注力              Boolean?  @checkbox @map("注力")
  "Sub-item"        String[]  @relation @map("Sub-item")
  似てるページ      String[]  @relation @map("似てるページ")
  日付              String?   @date @map("日付")
  責任者            Json?     @people @map("責任者")
  アーカイブ        Boolean?  @checkbox @map("アーカイブ")
  "OYKOT Timeline"  String[]  @relation @map("OYKOT Timeline")
  Action           String[]  @relation @map("Action")
  Objective進行度   String?   @formula @map("Objective進行度")
  "Parent item"     String[]  @relation @map("Parent item")
  Action進行度      String?   @formula @map("Action進行度")
}